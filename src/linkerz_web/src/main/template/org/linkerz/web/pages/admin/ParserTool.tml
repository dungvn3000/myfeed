<html t:type="adminLayout" title="LinkerZ Parser Tool"
      xmlns:t="http://tapestry.apache.org/schema/tapestry_5_3.xsd">

    <section class="span6">

        <t:zone t:id="downloadZone">
            <div class="row-fluid">
                <a href="${downloadUrl}">${title}</a>
                <div class="btn-group">
                    <t:actionlink t:id="nextTitleBtn" class="btn" zone="downloadZone">
                        <i class="icon-chevron-left"></i>
                    </t:actionlink>
                    <a class="btn">
                        <i class="icon-chevron-right"></i>
                    </a>
                </div>
            </div>
        </t:zone>

        <t:zone t:id="updateZone">
            <t:loop source="links" value="link">
                <div class="row-fluid">
                    <div class="span4">
                        <img src="http://vnexpress.net/${link.imgSrc}" alt="" width="80" height="80" style="padding-left:60px;"/>
                    </div>
                    <div class="span8">
                        <h5>
                            <a href="${link.url}">
                                ${link.title}
                            </a>
                        </h5>
                        <p>${link.description}</p>
                    </div>
                </div>
                <br/>
            </t:loop>
        </t:zone>

    </section>

    <section class="span6">

        <t:form t:id="downloadForm" class="well form-search" zone="downloadZone">
            <t:errors/>
            <fieldset>
                <lable class="control-label">
                    Url:
                </lable>
                <t:textfield value="downloadUrl" t:id="downloadUrlTxt"/>
                <button class="btn btn-primary" type="submit">
                    Download
                </button>
            </fieldset>
        </t:form>

        <t:form t:id="parserForm" class="well form-inline" zone="updateZone">
            <t:errors/>
            <fieldset>

                <div class="control-group">
                    <lable class="control-label">
                        UrlRegex:
                    </lable>
                    <div class="controls">
                        <t:textfield value="urlRegex" t:id="urlRegexTxt"/>
                    </div>
                </div>

                <div class="control-group">
                    <lable class="control-label">
                        Title:
                    </lable>
                    <div class="controls">
                        <t:textfield value="titleSelection" t:id="titleSelectionTxt" class="input-medium" placeholder="titleSelection"/>
                        <t:textfield value="titleAttName" t:id="titleAttNameTxt" class="input-small" placeholder="titleAttName"/>
                        <t:textfield value="titleMaxLength" t:id="titleMaxLengthTxt" class="input-small" placeholder="maxlength"/>
                    </div>
                </div>

                <div class="control-group">
                    <label class="control-label">
                        description:
                    </label>

                    <div class="controls">
                        <t:textfield value="descriptionSelection" t:id="descriptionSelectionTxt" class="input-medium" placeholder="descriptionSelection"/>
                        <t:textfield value="descriptionAttName" t:id="descriptionAttNameTxt" class="input-small" placeholder="descriptionAttName"/>
                        <t:textfield value="descriptionMaxLength" t:id="descriptionMaxLengthTxt" class="input-small" placeholder="descriptionMaxLength"/>
                    </div>
                </div>

                <div class="control-group">
                    <label class="control-label">
                        imgSelection:
                    </label>

                    <div class="controls">
                        <t:textfield value="imgSelection" t:id="imgSelectionTxt"/>
                    </div>
                </div>

                <div class="form-actions">
                    <button class="btn btn-primary" type="submit">Test</button>
                    <t:actionlink t:id="cancelBtn" class="btn">Cancel</t:actionlink>
                </div>
            </fieldset>
        </t:form>

        <t:form t:id="testForm" class="well form-search" zone="updateZone">
            <t:errors/>
            <lable class="control-label">Url Test:</lable>
            <t:textfield value="urlTest" t:id="urlTestTxt"/>
            <t:textfield value="numberOfUrl" t:id="numberOfUrl" class="input-mini" placeholder="numberOfUrl"/>
            <div class="form-actions">
                <button class="btn btn-primary" type="submit">
                    Download
                </button>
            </div>
        </t:form>

    </section>

</html>
