controllerSystem {
  include "common"

  akka {
      actor {
        provider = "akka.remote.RemoteActorRefProvider"
      }
  }
}

mongodb {
  default {
    db = "linkerz"
    host = "localhost"
  }
}

rabbitmq {
    default {
        host = "localhost"
    }
}

storm {
    nimbus {
        host = "192.168.1.100"
    }
}

server.monitor.path = "akka://serverSystem@127.0.0.1:2552/user/monitor"